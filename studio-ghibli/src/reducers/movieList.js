// import action variables


const initialState = {
    movies: [],
    error: '',
    isFetching: false
}

export const movieListReducer = (state = initialState, action) => {
    switch(action.type) {

        default:
            return state;
    }

}
